# GraphQL schema example
#
# https://gqlgen.com/getting-started/

type Patient {
  id: ID!
  name: String!
  orderId: ID
}

type Order {
  id: ID!
  message: String
}

type Query {
  patients: [Patient!]!
  orders: [Order!]!
}

input NewTodo {
  text: String!
  userId: String!
}

input NewPatient {
  name: String!
}

input UpdatePatient {
  name: String!
  orderId: ID
}

input NewOrder {
  message: String!
}

input UpdateOrder {
  message: String!
}

type Mutation {
  createPatient(input: NewPatient): Patient!
  updatePatient(input: UpdatePatient): Patient!
  createOrder(input: NewOrder): Order!
  updateOrder(input: UpdateOrder): Order!
}
